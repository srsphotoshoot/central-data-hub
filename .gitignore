credentials.json
token.json
.env
*.db
*.sqlite
__pycache__/
*.pyc
*.pyo
.DS_Store
data/ai_images/
venv/

*.log
*.db-shm
*.db-wal
*.index

# Matcher thumbnails: ~1000 JPEGs (49MB) used by the ORB keypoint re-ranker.
# Durable copy lives in the HF dataset repo (see services/hf_sync.py), not git.
data/matcher/thumbnails/
# Isolated test index built by scripts/test_experimental_matcher.py (10MB).
data/matcher_test_experimental/
# Timestamped state backups
*.bak-*
# Real-photo eval set built by scripts/build_real_photo_evalset.py (large JPEGs + embeddings)
data/matcher_eval/
